name: kateri
description: Antigenic Map Viewer
version: 0.1.20220810
homepage: https://antigenic-cartography.org

environment:
  sdk: '>=3.0.0 <4.0.0'

executables:
  kateri: kateri

dev_dependencies:
  lints: ^4.0.0
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.10.8
  path_provider: ^2.1.0
  intl: ^0.19.0
  universal_platform: ^1.0.0+1
  typed_data: ^1.3.0
  window_manager: ^0.3.0
  # side_navigation: ^1.0.2

  # open ace file
  file_picker: ^8.0.0

  # exportPdf
  file_saver: ^0.2.0
  brotli: ^0.6.0
  args: ^2.4.0
  file: ^7.0.0

flutter:
  uses-material-design: true
